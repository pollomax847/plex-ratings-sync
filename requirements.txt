# Dépendances Python pour Plex Scripts (webui + outils)
# À installer avec: pip install -r requirements.txt

# --- WebUI (Flask + serveur WSGI) -------------------------------------------
Flask>=3.0
gunicorn>=22.0

# --- Génération d'images (posters playlists) --------------------------------
Pillow>=10.0

# --- Édition de tags audio (mutagen) ----------------------------------------
mutagen>=1.47

# --- Classement bibliothèque audio (beets CLI) ------------------------------
beets>=2.2

# --- Dépendance externe: songrec (binaire) ----------------------------------
# Installé séparément sur l'hôte et bind-monté dans le container via
# docker-compose.yml. Pas un package pip.

# Note: les modules standard Python (sqlite3, json, pathlib, subprocess,
# argparse, logging, datetime, re, shutil) ne nécessitent rien à installer.
